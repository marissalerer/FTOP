<%= form_for(@time_entry) do |f| %>
  <% if @time_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_entry.errors.count, "error") %> prohibited this time_entry from being saved:</h2>

      <ul>
      <% @time_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label 'Name' %>(You must be registered.)<br/>
  <%= f.select(:coop_id, options_from_collection_for_select(@members, :coop_id, :full_name)) %>
<br/>
  <%= f.label 'Date Worked' %>
  <%= f.datepicker "date_worked" %>


  <%= f.label 'Hours Worked' %>
  <%= f.text_field :hours_worked %><br/>
  <%= f.label 'Supervisor' %>



<%=  
  f.select(:supervisor_id, options_from_collection_for_select(@supervisors, :id, :full_name)) 
%>

<br/>

  <%= f.label 'Description' %>
  <%= f.text_field :description %><br/>
  <%= f.label 'Comments' %>
  <%= f.text_field :comment %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
